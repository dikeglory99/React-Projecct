const Side = (props) =>{
    return (
        <div className="side_container">
            <h2>{props.title}</h2>
            <span>{props.des}</span>
        </div>
        
    );
    
}

export default Side;